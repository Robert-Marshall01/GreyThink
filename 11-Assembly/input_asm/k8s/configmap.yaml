# =============================================================================
# Kubernetes ConfigMap for input_asm
# Placeholder for environment configuration
# =============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: input-asm-config
  labels:
    app: input-asm
    version: "0.2.0"
    component: cli
data:
  # Application metadata (available as environment variables)
  APP_NAME: "input_asm"
  APP_VERSION: "0.2.0"
  APP_DESCRIPTION: "32-bit x86 Assembly number classifier"
  
  # Runtime configuration (placeholder for future use)
  LOG_LEVEL: "info"
