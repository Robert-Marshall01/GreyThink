# =============================================================================
# Grey AI Internal - Secrets
# =============================================================================
# IMPORTANT: In production, use a secrets manager (Vault, AWS Secrets Manager)
# or sealed-secrets instead of plain Kubernetes secrets.
#
# To generate base64 values:
#   echo -n 'your-password' | base64
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: grey-ai-secrets
  namespace: grey-ai
  labels:
    app.kubernetes.io/name: grey-ai
    app.kubernetes.io/part-of: grey-ai-internal
type: Opaque
data:
  # Base64 encoded: greyai_secret_change_me
  # CHANGE THIS IN PRODUCTION!
  POSTGRES_PASSWORD: Z3JleWFpX3NlY3JldF9jaGFuZ2VfbWU=
