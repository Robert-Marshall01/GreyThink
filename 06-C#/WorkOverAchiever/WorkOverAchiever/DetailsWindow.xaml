<?xml version="1.0" encoding="utf-8"?>
<Window
 x:Class="WorkOverAchiever.DetailsWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="using:WorkOverAchiever"
 Title="Details">

 <Window.SystemBackdrop>
 <MicaBackdrop />
 </Window.SystemBackdrop>

 <!-- Wrap the content in a ScrollViewer so the window shows a vertical scrollbar when needed -->
 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="0">
 <Grid x:Name="RootGrid" Padding="12" Background="{ThemeResource WindowBackgroundBrush}">
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="*" />
 </Grid.RowDefinitions>

 <TextBlock Grid.Row="0" Text="Details window" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,8" />

 <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
 <Button x:Name="DetailsClearButton" Content="Clear" Width="100" Click="DetailsClearButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
 <Button x:Name="DetailsCalculateButton" Content="Calculate" Width="120" Click="DetailsCalculateButton_Click" Style="{StaticResource PrimaryButtonStyle}" />
 </StackPanel>

 <Border Grid.Row="2" Background="{ThemeResource ControlBackgroundBrush}" BorderBrush="{ThemeResource ControlBackgroundBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,12,0,0">
 <StackPanel>
 <TextBlock Text="Result" Style="{StaticResource ResultTextStyle}" Margin="0,0,0,8" FontWeight="SemiBold" FontSize="16" />
 <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200">
 <TextBlock x:Name="DetailsResultTextBlock" Text="" TextWrapping="Wrap" Style="{StaticResource ResultTextStyle}" />
 </ScrollViewer>
 </StackPanel>
 </Border>
 </Grid>
 </ScrollViewer>
</Window>