# =============================================================================
# Kubernetes Secret for password_checker
# Store sensitive data like passwords to check
# NOTE: In production, use external secret management (e.g., Vault, AWS Secrets)
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: password-checker-secret
  namespace: default
  labels:
    app: password-checker
    app.kubernetes.io/name: password-checker
    app.kubernetes.io/version: "0.1.0"
type: Opaque
data:
  # Example password (base64 encoded) - REPLACE IN PRODUCTION
  # echo -n "ExamplePassword123!" | base64
  password: RXhhbXBsZVBhc3N3b3JkMTIzIQ==
