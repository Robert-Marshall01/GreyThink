apiVersion: v1
kind: ConfigMap
metadata:
  name: unit-converter-config
  labels:
    app: unit-converter
data:
  # Application configuration
  # Add environment-specific settings here if needed
  APP_NAME: "unit-converter"
  APP_VERSION: "1.0.0"
  LOG_LEVEL: "info"
